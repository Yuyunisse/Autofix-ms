server:
  port: 8094
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/reportesDB
    username: postgres
    password: 12345678
eureka:
  instance:
    hostname: localhost
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      default-zone: http://localhost:8761/eureka